# Copy to .env on the Nominatim instance (gitignored). Optional overrides.

# Postgres superuser password inside the Nominatim container. Change it.
NOMINATIM_PASSWORD=nominatim

# Import parallelism. Match to the vCPUs allocated to this VM.
THREADS=2
